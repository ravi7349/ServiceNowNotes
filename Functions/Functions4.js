//taking input and return  output
// 👉 Function takes input and also returns result
function add(x, y) {
    let c = x + y;
    return c;
}

let res = add(1000, 2000);
console.log(res); // Output: 3000
